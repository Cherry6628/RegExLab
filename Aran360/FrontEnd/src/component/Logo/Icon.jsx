export default function Icon({ size = "44px" }) {
    return (
        <span
            className="material-symbols-outlined"
            style={{
                color: "var(--action-color)",
                fontSize: "35px",
            }}
        >
            frame_bug
        </span>
    );
}
